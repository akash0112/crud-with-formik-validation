import React from "react";

const Navbar = () => {
  return (
    <nav className="navbar navbar-dark bg-primary">
      <a className="container-fluid  navbar-brand">Test Task</a>
    </nav>
  );
};

export default Navbar;
